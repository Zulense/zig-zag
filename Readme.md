# z-agent

1. clone the repo git clone https://github.com/Zulense/z-agent.git

2. activate virtual envirnoment. source .z-agent/Scripts/activate

3. install the dependencies. pip install -r requirement.txt

4. create .env file in base folder and put the gemni API_KEY (make sure name are matched)

5. run the python file. python main.py